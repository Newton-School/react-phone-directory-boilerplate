import { useState, useEffect } from "react";

const useTrieSearch = (trie, searchTerm) => {
  const [searchResults, setSearchResults] = useState([]);

  // useEffect(() => {

  // });

  return searchResults;
};

export default useTrieSearch;
